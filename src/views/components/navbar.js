let Navbar = {
  render: async () => {
    let view = /*html*/ `
        `;
    // Dentro de las comillas invertidas va la maquetación (html) del navbar
    return view;
  },
  after_render: async () => {}
};
export default Navbar;
